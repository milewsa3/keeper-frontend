import React from 'react';
import { Typography } from '@mui/material';

const NotFound = () => {
  return (
    <Typography textAlign={"center"} color={'text.primary'} marginTop={10} variant={"h3"}>Page not
      found! 😔</Typography>
  );
};

export default NotFound;